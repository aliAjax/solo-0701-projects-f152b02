# 方块消除

使用Vue3、Vite、TypeScript和Konva实现一个方块消除小游戏，玩家点击相邻同色方块进行消除，消除后上方方块下落补位。需要有目标分数、剩余步数、连消加分、失败和胜利状态，先做单关卡闭环。

- 技术栈：Vue3、Vite、TypeScript、CSS Grid
- 启动：`npm install && npm run dev`
- 构建：`npm run build`

当前版本实现功能最小闭环，适合作为后续扩展关卡、素材、排行榜和音效的基础。
